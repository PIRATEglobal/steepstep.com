# Steepstep launch ledger

| Requirement | Status | Evidence / next check |
| --- | --- | --- |
| PIRATE GmbH is the operator | Verified | `src/pages/impressum.astro` uses the legal details from pirate.global. Confirm with counsel before relying on the page. |
| Privacy page and no tracking claim | Implemented | `src/pages/datenschutz.astro` documents no analytics, ads or tracking cookies, All-Inkl technical logs, contact form data and rights. No analytics or tracker scripts are present in the built site. |
| Private client communication | Implemented | The homepage, Ablauf, FAQ and AGB explain a pre-agreed protected communication route for active programme clients between appointments, with no public messenger accounts. The reusable package cards now explicitly include a protected message route for the four and six session active support packages, without promising a platform, response time or emergency access. |
| Messenger support in active packages | Implemented | Four and six session packages include a protected, pre-agreed messaging route for questions between appointments during active support. No platform, account or response time is promised publicly; free orientation and paid assessment do not include it. |
| Visible “Vorschau” removed | Verified in source | `rg -n -i 'vorschau' src` returns no matches after this pass. |
| Contact form to contact@steepstep.com | Implemented, delivery proof follow-up open | `npm run dev` starts a local PHP companion on `127.0.0.1:8788` and proxies `/api` through Astro. Set `STEEPSTEP_PHP_PORT` to override the local port. Live token and rejection smoke pass; no real message was sent during verification. |
| Named contact and expert profile | Implemented, credential confirmation pending | `src/pages/ueber-uns.astro` and `src/pages/kontakt.astro` name Natalja Krol as a physiotherapist with a sports physiotherapy focus, based on the supplied Powerflow source and the user confirmation. Confirm credentials and Swiss practice authorization before relying on those claims publicly. |
| CHF pricing | Implemented, enquiry only | `/preise/` publishes CHF 149 Ersttermin, CHF 549 Begleitung, CHF 749 Fortschritt and the free 20-minute orientation conversation with inclusions; contact is nonbinding and no payment is taken on site. |
| End prices and MWST clarity | Implemented, accounting classification to keep aligned | Shared offer component, `/preise/`, homepage and AGB state that CHF amounts are total prices, any applicable MWST is included, and no additional taxes or fees are added. ESTV distinguishes exempt physiotherapeutic treatment from taxable wellbeing/performance services; see `docs/legal/2026-09-swiss-agb-research.md`. |
| Swiss AGB | Implemented, internal legal follow-up | `/agb/` publishes readable Swiss-facing terms covering the live prices, booking confirmation, package validity, fair cancellation, refunds, technical interruptions, safety, privacy and consumer rights. Lawyer review remains an internal follow-up and is not a release blocker under the user-authorized launch scope. |
| Finished page and brand story | Implemented | Retained routes have substantive Swiss-German content, clear CHF enquiry pricing, team context, sport depth, legal pages, contact path and the steepstep story. |
| Benchmark-informed service depth | Implemented | `/so-funktionierts` now explains the care journey, concrete outputs, preparation and online suitability boundaries. Homepage method section explains the repeatable understand, test, observe and adapt cycle. `/ueber-uns` adds the expert-group principles and Natalja context without unsupported claims. |
| Critical review | Passed | Final code gate passed Astro check, Astro build, contact security tests and `git diff --check`; route, imagery, motion, accessibility and no-tracking review completed. |
| Sport route depth decision | Implemented | Retain `/sport/`, `/sport/laufen/`, and `/sport/skifahren/`; remove thin Fussball and Wandern routes. See `docs/research/switzerland/2026-09-competitor-page-depth.md`. |
| Canonical and sitemap | Implemented | `astro.config.mjs` sets `https://steepstep.com`; `public/sitemap.xml` contains only retained, indexable routes and is linked from `public/robots.txt`. |
| Sport imagery and motion | Implemented | Laufen, Skifahren and the Sport hub use distinct registered images. Shared entrance, breathing-scale, hover and reduced-motion rules are in place. |
| Sports condition cluster | Implemented, practitioner review pending | Added `/beschwerden/` plus five symptom-led pages with sourced clinical framing, online-care boundaries, illustrative progression phases, safety routes, FAQs, internal links and page-level MedicalWebPage data. See `docs/research/switzerland/2026-09-sports-conditions-page-register.md`; practitioner must review before relying on clinical copy. |
| FAQ depth and evidence mapping | Implemented, practitioner review pending | Rewrote all general, pricing, running, skiing and condition FAQs with direct decision guidance, practical progression logic and safety boundaries. The complete question-to-evidence map and source register is in `docs/research/switzerland/2026-09-faq-evidence-matrix.md`. |
| GitHub push and live verification | Verified | Commits `db1590c` and `0cabb93` are pushed to `PIRATEglobal/steepstep.com`; publish runs `34720040498` and `34720343072` succeeded and uploaded only `/steepstep.com/`. |
| Shared pricing source and display | Implemented, browser proof follow-up | `src/data/offers.ts` owns the CHF values and per-session arithmetic; the shared component and explanatory pages interpolate the same source. Responsive long-label handling is implemented. |
| Navigation CTA and submenu dismissal | Implemented, browser proof pending | The shared header remains available while scrolling with the same visible contact CTA. The complaints submenu starts closed, keeps the active indicator, and closes on outside click, Escape, or navigation. Verify the compact 360/390/768/1024 layouts in the final browser pass. |
| Mobile menu control | Implemented, browser proof pending | The mobile trigger now has a 48px-class pill target, CSS three-line icon with open-state morph, visible focus/hover treatment, and no Unicode glyph dependency. Verify at 300/360/390px for no overflow. |
| Recommendation panel depth and hierarchy | Implemented, practitioner review pending | Homepage specimen now gives the action content the visual weight: a fictional running example covers goal, starting point, a short observation window, next-day response, checkpoint and referral boundary. The sequence line is explanatory rather than decorative; wording is illustrative and must be checked by a named practitioner before clinical reliance. |
| Clinical voice and source handling | Implemented, practitioner review pending | Public clinical answers now speak from the team’s measured practice approach. Condition pages present sources as further reading, without dated editorial boilerplate or routine “guidelines say” phrasing; the source register remains available for traceability. |
| Route animation and imagery audit | Open, browser proof required | Source review finds route-specific condition and sport imagery, semantic diagrams, hover states and reduced-motion rules. Verify the required responsive viewports in a supervised browser pass. |
| Shared motion language | Implemented, browser proof required | All article routes now receive opt-in section, image and process-diagram entrance motion through the shared layout observer. Selected large editorial, sport and condition images use a 20-second, 1.2% breathing scale, while shared content cards gain restrained pointer hover lift and buttons/images/links retain tactile states. No content is hidden without JavaScript, and reduced-motion or missing IntersectionObserver keeps everything visible. |
| Shared motion and navigation visual verification | Verified locally | Supervised CUA pass on `/sport/` at desktop width confirmed the offscreen feature image transform `rotate(-.35deg) translateY(16px)`, reset to `none` after `.is-visible`; pointer hover computed approximately `scale(1.017)` with independent breathing scale `1.0118` and filter change. Closed and open Beschwerden caret screenshots showed centered geometry and an aligned submenu. |
| Contact preview runtime | Resolved, delivery proof follow-up open | Persistent local 4322 plus PHP 8788 returns a valid token and challenge. Live token 200, foreign origin 403, invalid POST 400 and direct secret 403. No real delivery was sent. |
| Page audit closure | Implemented with browser-proof follow-up | Build, route, SEO, local and live security checks pass. Manual viewport and keyboard browser proof remains a quality follow-up. |
| Portable Resonance framework and project memory | Published | Portable `.resonance/` memory and requested framework material were included through an explicit allowlist. Secret/private-data scan is clean; `.env*`, caches and generated artifacts are excluded. |
| Sport SEO/GEO/AEO hub | Implemented and deployed | `/sport/` now answers Swiss online sport physiotherapy intent early, gives concrete load logic for six sports, links to the five complaint guides and explains the service. Research and plan are recorded in the linked documents. |

| Final operations audit | Passed with browser follow-up | No P0/P1 code findings remain. Source/build/security/SEO checks and live smoke passed; responsive browser proof remains a quality follow-up. |
| System health | Local score 100/100, operational readiness pending | `docs/reviews/2026-09-system-health.md` records passing local vitals and open `ENV_FRAGILE`, `TEST_SHALLOW` and `STATE_DRIFT_DETECTED` flags. The local formula is kept separate from live release readiness. |
| Ponytail audit | Lean | `docs/reviews/2026-09-ponytail-audit.md` finds no justified deletions or dependency cuts. |
| Release 8d5b310 / Actions 34717599792 | Deployed static site | GitHub Actions build and FTPS deploy both succeeded on 12 September 2026. Workflow uses required TLS with relaxed certificate verification and uploads only to `/steepstep.com/`. Live smoke returned HTTP 200 for `/`, `/preise/`, `/kontakt/`, `/beschwerden/`, `/sport/`, `/ueber-uns/`, `/fragen/`, `/sitemap.xml` and `/robots.txt`. |
| Release db1590c / Actions 34720040498 | Deployed complete SEO, sport, pricing and motion update | GitHub Actions build and FTPS deploy succeeded on 12 September 2026. The workflow uploaded only to `/steepstep.com/` using `--ssl-reqd --insecure`. Live routes `/`, `/sport/`, `/preise/`, `/kontakt/`, `/beschwerden/`, `/ueber-uns/`, `/fragen/`, `/sitemap.xml` and `/robots.txt` returned HTTP 200. |
| Live contact endpoint after release | Resolved | The generated secret include was corrected and direct requests now return 403. Live allowed-origin token 200, foreign origin 403 and invalid POST 400. |
| Live contact smoke after 0cabb93 | Verified, delivery proof follow-up open | Allowed-origin token 200 with valid token and CAPTCHA, foreign origin 403, invalid POST 400, direct secret 403 with empty body. No controlled successful mail was sent. |
| Release dd4299b / Actions 34720621392 | Deployed final clinical refinements | Publish completed successfully to `/steepstep.com/` over FTPS. Representative live routes `/`, `/sport/`, `/sport/laufen/`, `/beschwerden/achillessehne/`, `/beschwerden/umgeknickter-knoechel/`, `/preise/` and `/kontakt/` returned HTTP 200. Allowed-origin token returned 200 with valid token and CAPTCHA; direct secret returned 403 with an empty body; foreign origin returned 403; invalid POST returned 400. No real message was sent, per release safety boundary. |
| Release candidate 08c8c44 / pre-publish gate | Verified locally and against live contact boundary | `npm run check` reports 0 errors, 0 warnings and 0 hints; `npm run build` produces 20 static pages; `php tests/contact-security.php` passes; `git diff --check` passes; source/public/tests/scripts contain no unfinished TODO/FIXME items. Live representative routes return HTTP 200, allowed-origin token returns 200 with a CAPTCHA, foreign origin returns 403, invalid `/api/contact.php` POST returns 400, and direct `/api/form-secret.php` access returns 403 with 0 bytes. No successful mailbox receipt was claimed or tested. |
| Homepage and sport-grid refinement | Implemented, browser proof pending | The sport complaint guide grid now lays out five cards as a complete 3-card row plus 2-card row at desktop widths, with one-column mobile fallback; the output cards use three equal desktop columns. `npm run check`, `npm run build`, PHP contact security tests and `git diff --check` pass. The full 360/390/768/1024 responsive, keyboard and reduced-motion matrix remains unverified. |
| Publish run 34745635574 | Cancelled before release | Build passed for `8d7d49c`, but the FTPS upload was cancelled after the homepage layout issue was identified. This run is not production release proof. |
| Contact-page refinement | Implemented and visually verified | The contact page removes the duplicate framing, tightens the opening composition and keeps the protected form prominent. Supervised browser checks at 1562px and 390px show no horizontal overflow and a visible CAPTCHA. `npm run check`, `npm run build`, PHP contact security tests and `git diff --check` pass. Keyboard and reduced-motion coverage remains open. |
| Publish run 34745818219 | Cancelled before release | The run was cancelled during setup after a further contact-page design issue was reported. This run is not production release proof. |
| Release a74ea49 / Actions 34746224456 | Deployed to All-Inkl | Build and deploy both succeeded for `a74ea49`. The FTPS workflow uploaded the static artifact only under `/steepstep.com/`. After deployment, `/`, `/kontakt/`, `/sport/`, `/preise/`, `/beschwerden/`, `/sitemap.xml` and `/robots.txt` returned HTTP 200. Contact API smoke returned allowed-origin token 200, foreign origin 403, direct secret 403 and invalid POST 400. Mailbox receipt, practitioner/legal/image-rights confirmations and the full keyboard/reduced-motion matrix remain external follow-ups. |
